---
apiVersion: v1
kind: Service
metadata:
  name: mvc-service
  labels:
    app: mvc-service
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: mvc-service
 
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: mvc-service-blue
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: mvc-service
        version: blue
    spec:
      containers:
      - name: mvc-service
        image: mvc-service:blue
        imagePullPolicy: Never
        ports:
        - containerPort: 9080
